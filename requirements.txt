# Ebbinghaus Anywhere 运行依赖
# Python >= 3.10

Django==4.2.30
django-environ==0.14.0
django-dirtyfields==1.9.9
# 注意:DRF 3.18+ 要求 Django>=5.2,与本项目的 Django 4.2 LTS 冲突;
# 3.17.2 是支持 Django 4.2 的最新版本。升级 Django 前请勿单独升 DRF。
djangorestframework==3.17.2
openpyxl==3.1.5
requests==2.34.2
Markdown==3.10.3

# ---- 以下仅 NAS 副本容器需要(主库 PA 与本地开发不安装)----
# 生产级 WSGI 服务器:
# gunicorn==23.0.0
# 容器内静态文件服务(settings 仅在 EAW_ROLE=replica 时启用其中间件):
# whitenoise==6.12.0

# 仅 scripts/ 下的一次性数据整理脚本需要(正常部署可省略):
# pandas
# openai
